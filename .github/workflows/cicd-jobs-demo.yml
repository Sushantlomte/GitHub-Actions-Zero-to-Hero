name: CI/CD Pipeline

on:
  push:
    branches:
      - main  # Trigger pipeline on push to the main branch
  pull_request:
    branches:
      - main  # Trigger pipeline on pull requests to the main branch

jobs:
  build:
    name: Build Job
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Placeholder for installing dependencies
      - name: Install dependencies
        run: echo "Installing dependencies..." # Replace with your actual dependency installation commands

      # Step 3: Placeholder for running tests
      - name: Run tests
        run: echo "Running tests..." # Replace with your actual test commands
  deploy-dev:
            name: Deploy Job dev env
            runs-on: ubuntu-latest
            needs: build  # Ensures this runs after the 'build' job
        
            steps:
              # Step 1: Check out the repository
              - name: Checkout repository
                uses: actions/checkout@v3
        
              # Step 2: Placeholder for deployment
              - name: Deploy to environment
                run: echo "Deploying to dev env ..." # Replace with your actual deployment commands
  deploy-qa:
    name: Deploy Job qa env
    runs-on: ubuntu-latest
    needs: build  # Ensures this runs after the 'build' job

    steps:
      # Step 1: Check out the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Placeholder for deployment
      - name: Deploy to environment
        run: echo "Deploying to qa..." # Replace with your actual deployment commands
